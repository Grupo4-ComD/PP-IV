import { NextResponse } from 'next/server'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export async function GET() { try { const t = await prisma.ticketReclamo.findFirst(); if (t) { await prisma.presupuestoVotacion.createMany({ data: [ { ticketId: t.id, proveedorNombre: 'Plomeria San Justo', montoTotal: 150000, detalle: 'Reparacion de cañeria principal en cochera', estado: 'en_votacion' } ] }); } const hoy = new Date(); const inicio = new Date(hoy); inicio.setDate(hoy.getDate() - hoy.getDay() + 1); const fin = new Date(inicio); fin.setDate(inicio.getDate() + 6); await prisma.limpiezaRotativa.createMany({ data: [ { unidadIdAsignada: BigInt(7), semanaInicio: inicio, semanaFin: fin, estado: 'cumplido' } ] }); return NextResponse.json({ message: 'Datos de vecino inyectados exitosamente' }); } catch (error) { return NextResponse.json({ error: String(error) }, { status: 500 }); } }
