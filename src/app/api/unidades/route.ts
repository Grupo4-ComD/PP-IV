import { NextResponse } from 'next/server'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export async function GET() { try { const unidades = await prisma.unidad.findMany({ orderBy: { numeroUf: 'asc' } }); const data = unidades.map(u => ({ id: u.id.toString(), numero_uf: u.numeroUf, piso_depto: u.pisoDepto, propietario_nombre: u.propietarioNombre, email: u.email, coeficiente_prorrateo: Number(u.coeficienteProrrateo) })); return NextResponse.json(data); } catch (err) { return NextResponse.json({ error: String(err) }, { status: 500 }); } }
