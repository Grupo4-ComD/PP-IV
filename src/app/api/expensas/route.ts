import { NextResponse } from 'next/server'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export async function GET() { try { const expensas = await prisma.expensa.findMany({ include: { unidad: true }, orderBy: [ { periodoAnio: 'desc' }, { periodoMes: 'desc' }, { unidadId: 'asc' } ] }); const data = expensas.map(e => ({ id: e.id.toString(), unidad_id: e.unidadId.toString(), periodo_mes: e.periodoMes, periodo_anio: e.periodoAnio, monto_ordinario: Number(e.montoOrdinario), recargo_mora: Number(e.recargoMora), total_pagar: Number(e.totalPagar), fecha_vencimiento: e.fechaVencimiento.toISOString().split('T')[0], estado: e.estado, fecha_pago: e.fechaPago ? e.fechaPago.toISOString() : null, unidades: e.unidad ? { numero_uf: e.unidad.numeroUf, piso_depto: e.unidad.pisoDepto, propietario_nombre: e.unidad.propietarioNombre } : null })); return NextResponse.json(data); } catch (err) { return NextResponse.json({ error: String(err) }, { status: 500 }); } }
