import { NextResponse } from 'next/server'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export async function GET() { try { const tickets = await prisma.ticketReclamo.findMany({ include: { unidad: true }, orderBy: { id: 'desc' } }); const data = tickets.map(t => ({ id: t.id.toString(), unidad_id: t.unidadId.toString(), titulo: t.titulo, descripcion: t.descripcion, categoria: t.categoria, estado: t.estado, fecha_creacion: t.fechaCreacion.toISOString(), unidades: t.unidad ? { numero_uf: t.unidad.numeroUf, piso_depto: t.unidad.pisoDepto, propietario_nombre: t.unidad.propietarioNombre } : null })); return NextResponse.json(data); } catch (err) { return NextResponse.json({ error: String(err) }, { status: 500 }); } }
